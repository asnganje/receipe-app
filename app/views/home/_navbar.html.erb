  <nav class="flex items-center justify-between mb-8 w-full">
    <h1 class="text-4xl uppercase hidden md:block border border-y-amber-500 font-extrabold text-gray-900 hover:italic">
      <%= link_to root_path do%>
        <%= inline_svg_tag("recipe.svg", class:"cursor-pointer hidden md:block size-10 rounded-lg")%>
      <%end%>
    </h1>
    <div class="flex items-center justify-between gap-6 w-full md:w-auto">
      <% if signed_in? %>
        <div class="bg-orange-500 p-2 rounded-md capitalize font-bold text-gray-200 hover:bg-orange-600">
          <%= link_to destroy_user_session_path, data: {turbo_method: :delete} do%>
            <span class="flex items-center gap-2">Logout <%= inline_svg_tag("arrow-right-circle.svg", class:"size-6")%></span>
          <%end%>
        </div>
        <div class="bg-gray-700 hover:bg-gray-600 p-2 rounded-md capitalize font-bold text-gray-200">
          <%= link_to "Dashboard", foods_path, class:"bg-gray-800 hover:bg-gray-700" %>
        </div>
      <%else%>
        <div class="bg-orange-500 p-2 rounded-md capitalize font-bold text-gray-200 hover:bg-orange-600">
          <%= link_to new_user_session_path do%>
            <span class="flex items-center gap-2">Login <%= inline_svg_tag("arrow-right-circle.svg", class:"size-6")%></span>
          <%end%>
        </div>
        <div class="bg-gray-700 hover:bg-gray-600 p-2 rounded-md capitalize font-bold text-gray-200">
          <%= link_to "Signup", new_user_registration_path %>
        </div>
      <%end%>
    </div>
  </nav>